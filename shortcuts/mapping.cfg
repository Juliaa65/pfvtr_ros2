# Parameters for pfvtr MapMaker goals (see ../action/MapMaker.action).
# Read by mapping.sh and bidirectional_mapping.sh via yaml_to_env.py.
#
# bidirectional_mapping.sh derives the rear-camera map's name by appending
# "_back" to map_name below — there's no separate key for it.

map_name: map_forward
save_imgs_for_viz: true
map_step: 1.0
source_map: ""
